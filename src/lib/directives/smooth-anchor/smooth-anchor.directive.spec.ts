import { SmoothAnchorDirective } from './smooth-anchor.directive';

describe('SmoothAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new SmoothAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
