import { TargetFragmentDirective } from './target-fragment.directive';

describe('TargetFragmentDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetFragmentDirective();
    expect(directive).toBeTruthy();
  });
});
