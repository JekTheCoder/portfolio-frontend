import { OpenSidenavDirective } from './open-sidenav.directive';

describe('OpenSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
