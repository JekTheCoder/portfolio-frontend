import { SidenavCloserDirective } from './sidenav-closer.directive';

describe('SidenavCloserDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavCloserDirective();
    expect(directive).toBeTruthy();
  });
});
